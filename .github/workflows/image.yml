name: Build docker WordPress image

on:
  workflow_dispatch:
  schedule:
    - cron: "0 4 * * */7"
  push:
    branches: master
    paths:
      - .github/workflows/image.yml  # This workflow file itself
      - .dockerignore
      - Dockerfile
      - Dockerfile.cron
      - hack/docker-bake-common.hcl
      - hack/docker-bake-web.hcl
      - hack/docker-bake-cron.hcl
      - rootfs/**
      - src/**
      - patches/**

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      -
        name: checkout code
        uses: actions/checkout@v5
      -
        name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      -
        name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          config-inline: |
            [worker.oci]
              max-parallelism = 16
      -
        name: Login to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
      -
        name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{github.actor}}
          password: ${{secrets.PAT_TOKEN}}
      -
        name: Free Disk Space (Ubuntu)
        uses: BRAINSia/free-disk-space@v2.1.2
        with:
          tool-cache: false
          android: true
          dotnet: true
          haskell: true
          large-packages: false
          docker-images: false
          swap-storage: true
      -
        name: Await successful tests
        uses: lewagon/wait-on-check-action@v1.4.1
        with:
          ref: ${{ github.sha }}
          check-name: tests
          repo-token: ${{ secrets.GITHUB_TOKEN }}
          wait-interval: 20
      -
        name: Build and push images
        uses: docker/bake-action@v6.10.0
        with:
          files: |
            hack/docker-bake-common.hcl
            hack/docker-bake-web.hcl
            hack/docker-bake-cron.hcl
          push: true